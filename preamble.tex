% =========================
% Configuración de Página y Formato
% =========================
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish, es-tabla]{babel}
\usepackage[margin=2.5cm]{geometry} 
\usepackage{parskip} 
\usepackage{enumitem}
\setlist[enumerate]{label=\arabic*., ref=\arabic*} 
\usepackage{csquotes}
\usepackage{float}

% =========================
% Bibliografía 
% =========================
\usepackage[
    backend=biber,      
    style=authoryear,  
    citestyle=authoryear,
    natbib=true,        
    maxcitenames=2,     
    uniquelist=false,
    sorting=nyt         
]{biblatex}

% =========================
% Matemáticas 
% =========================
\usepackage{amsmath, amsfonts, mathtools, amsthm, amssymb, mathrsfs, stmaryrd, bm}

% =========================
% Teoremas 
% =========================
\usepackage[dvipsnames]{xcolor}
\usepackage{thmtools}
\usepackage[framemethod=TikZ]{mdframed}

\mdfsetup{skipabove=1em, skipbelow=0em}

\declaretheoremstyle[
    headfont=\bfseries\sffamily\color{MidnightBlue},
    bodyfont=\normalfont,
    mdframed={linewidth=2pt, rightline=false, topline=false, bottomline=false, linecolor=MidnightBlue, backgroundcolor=MidnightBlue!5}
]{styleDef}

\declaretheoremstyle[
    headfont=\bfseries\sffamily\color{ForestGreen},
    bodyfont=\normalfont,
    mdframed={linewidth=2pt, rightline=false, topline=false, bottomline=false, linecolor=ForestGreen, backgroundcolor=ForestGreen!5}
]{styleThm}

\declaretheoremstyle[
    headfont=\bfseries\sffamily, 
    bodyfont=\normalfont,
    numbered=no,
    spaceabove=1em,
    spacebelow=1em
]{styleNote}

% Declaración de entornos
\declaretheorem[style=styleDef, name=Definición, numberwithin=section]{definicion}
\declaretheorem[style=styleDef, name=Axioma, sibling=definicion]{axioma}
\declaretheorem[style=styleThm, name=Teorema, sibling=definicion]{teorema}
\declaretheorem[style=styleThm, name=Proposición, sibling=definicion]{proposicion}
\declaretheorem[style=styleThm, name=Lema, sibling=definicion]{lema}
\declaretheorem[style=styleThm, name=Corolario, sibling=definicion]{corolario}
\declaretheorem[style=styleNote, name=Ejemplo, sibling=definicion]{ejemplo}
\declaretheorem[style=styleNote, name=Nota, numbered=no]{nota}
\declaretheorem[style=styleNote, name=Observación, numbered=no]{observacion}

\renewenvironment{proof}{{\noindent\bfseries\sffamily Demostración.}}{\qed\vspace{1em}}

% =========================
% Hipervínculos 
% =========================
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=MidnightBlue,
    citecolor=ForestGreen,
    urlcolor=blue
}

% =========================
% Figuras externas
% =========================
\usepackage{import}
\usepackage{xifthen}
\usepackage{pdfpages}
\usepackage{transparent}

\newcommand{\incfig}[1]{%
    \def\svgwidth{\columnwidth}
    \import{./figures/}{#1.pdf_tex}
}

% =========================
% Clase...
% =========================
\newcommand{\clase}[3]{%
    \ifthenelse{\isempty{#3}}{%
        \def\@clase{Clase #1}%
    }{%
        \def\@clase{Clase #1: #3}%
    }%
    \subsection*{\@clase}
    \marginpar{\small\textsf{#2}}
}

% ========================
% Newthough
% ========================

\newcommand{\newthought}[1]{%
   \addvspace{1.5\baselineskip plus 0.5\baselineskip minus 0.2\baselineskip}%
   \noindent\textsc{#1}%
}

\author{César Pérez Amador}

